# 🚀 Установка и использование Whisper на Windows

Скрипты и инструкция для быстрой установки **Whisper** — нейросетевой модели распознавания речи от OpenAI.

## ✨ Возможности

- 🎤 Распознавание речи из аудио- и видеофайлов
- 🌍 Поддержка множества языков (включая русский)
- ⚡ Работа на CPU и GPU (ускорение с CUDA)
- 💻 Простая установка и использование через командную строку
- 🐍 Возможность запуска через `python main.py`

---

# 📦 Способ 1. Автоматическая установка (рекомендуется)

Если в проекте есть файл **`install_whisper.py`**, просто запустите его:

```bash
python install_whisper.py
```

Скрипт автоматически установит необходимые компоненты и подготовит окружение.

---

# 🔧 Способ 2. Ручная установка

## 1. Установите Python

Скачайте Python версии **3.10–3.12** с официального сайта:

👉 https://www.python.org/downloads/windows/

Во время установки обязательно поставьте галочку **"Add Python to PATH"**.

Проверьте установку:

```bash
python --version
```

---

## 2. Установите FFmpeg

FFmpeg необходим для обработки аудио и видео.

### 📥 Шаг 1. Скачайте FFmpeg

Перейдите на сайт:

👉 https://www.gyan.dev/ffmpeg/builds/

В разделе **Release Builds** скачайте архив:

- `ffmpeg-release-full.7z` *(рекомендуется)*

---

### 📂 Шаг 2. Распакуйте архив

Установите **7-Zip**, если он отсутствует.

Распакуйте архив, например, в:

```text
C:\Tools\ffmpeg
```

Структура будет выглядеть примерно так:

```text
C:\Tools\ffmpeg\
└── ffmpeg-<версия>\
    └── bin\
        └── ffmpeg.exe
```

---

### ⚙️ Шаг 3. Добавьте FFmpeg в PATH

1. Откройте:

   **Панель управления → Система → Дополнительные параметры системы**

2. Нажмите **Переменные среды**.

3. В разделе **Системные переменные** выберите **Path** → **Изменить**.

4. Добавьте путь до папки **bin**, например:

```text
C:\Tools\ffmpeg\ffmpeg-7.1.1-full_build\bin
```

5. Нажмите **ОК**.

---

### ✅ Шаг 4. Проверьте установку

```bash
ffmpeg -version
```

Если появилась информация о версии FFmpeg — всё настроено правильно.

---

## 3. Установите Whisper

```bash
pip install git+https://github.com/openai/whisper.git
```

Если появляется ошибка, убедитесь, что установлен **Git**.

---

## 4. (Необязательно) Установите PyTorch с поддержкой CUDA

Для видеокарт NVIDIA:

```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

Если используется другая версия CUDA, замените `cu118` на нужную (например `cu121`).

---

# ✅ Проверка установки

Выполните тестовое распознавание:

```bash
whisper audio.mp3 --model tiny
```

При первом запуске выбранная модель автоматически загрузится из интернета.

---

# 🐍 Запуск через `main.py`

Если проект содержит файл **`main.py`**, можно использовать его вместо команды `whisper`.

Запуск:

```bash
python main.py
```

Чтобы посмотреть список поддерживаемых параметров:

```bash
python main.py --help
```

Использование `main.py` позволяет запускать распознавание через подготовленный интерфейс проекта без необходимости вручную вводить длинные команды Whisper.

---

# 📋 Доступные модели

От самой быстрой к самой точной:

```text
tiny
base
small
medium
large
```

---

# 📥 Автоматическая установка

Если в проекте имеется скрипт `install_whisper.py`, рекомендуется использовать именно его.

---

# ⚠️ Важно

- При первом запуске модель автоматически загрузится из интернета (около **1–3 ГБ**).
- Для работы на GPU необходимы драйверы NVIDIA и CUDA.
- Python и FFmpeg должны быть добавлены в переменную **PATH**.
- При ошибках `ffmpeg` проверьте установку и перезапустите терминал.

---

# 🛠 Используемые технологии

- Whisper
- PyTorch
- FFmpeg
- Python
